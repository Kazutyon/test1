# かんたん公開鍵暗号 動くRSA実験室

小さなRSA暗号を使って、公開鍵・秘密鍵・暗号化・復号の流れをブラウザ上で体験できる教材です。

## 公開URL

https://kazutyon.github.io/test1/

## 内容

- 素数 `p` / `q` を変更できます。
- 公開指数 `e` を変更できます。
- 送りたい数字 `M` を変更できます。
- `n`、`phi(n)`、公開鍵、秘密鍵 `d`、暗号文、復号結果が自動で変わります。
- 暗号化と復号の計算過程を追えます。

## 注意

これは学習用の小さなRSAデモです。実際の暗号用途には使いません。
